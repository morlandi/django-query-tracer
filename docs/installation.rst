============
Installation
============

At the command line::

    $ easy_install django-query-tracer

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-query-tracer
    $ pip install django-query-tracer
